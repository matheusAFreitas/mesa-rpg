{
  "name": "mesa-do-mestre",
  "version": "1.0.0",
  "description": "RPG Game Master tool - Multiverso Caótico",
  "scripts": {
    "start": "npm start --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "prod:back": "npm start --prefix server",
    "prod:front": "npm run build --prefix client && npm run preview --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
